import { ContentSection, HeroSection } from "@/components/Pages/About";

const About = () => {
    return(
        <>
            <HeroSection/>
            <ContentSection/>
        </>
    );
};

export default About;
